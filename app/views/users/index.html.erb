<%= render 'books/profile', user: @user %>

<h1>Users</h1>
<% @users.each do |user| %>
<div>
  <%= image_tag user.get_profile_image(100, 100) %>
  <p>Name: <%= user.name %></p>
  <p><%= link_to "Show", user_path(user.id) %></p>
</div>
<% end %>

<%= render 'creation', book: @book %>